import { ethers } from "ethers";

export const GwEI = ethers.BigNumber.from("10").pow(9);
export const WEI = ethers.BigNumber.from("10").pow(9);
export const ETHER = ethers.BigNumber.from("10").pow(9);
